/** @type {import('next').NextConfig} */
const nextConfig = {
  // i18n: {
  //   locales: ["sr-sp"],
  //   defaultLocale: "sr-sp",
  // },
  trailingSlash: true,
  output: "export",
};

module.exports = nextConfig;
